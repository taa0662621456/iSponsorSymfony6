{% extends '@Shop/layout.html.twig' %}
{% trans_default_domain 'ShopBundle' %}

{% block title %}
    {{ 'order.thank.you'|trans({},'order') }}
{% endblock title %}

{% block page_content -%}
    <div class="row">
        <div class="col-md-10 col-md-offset-1">
            <h2>{{ 'order.thank.you.handleorder'|trans({},'order') }}</h2>
            <p>
                <a href="{{ path('index_main') }}">
                    {{ 'order.thank.you.continue'|trans({},'order') }}
                </a>
            </p>
        </div>
    </div>
{% endblock page_content %}

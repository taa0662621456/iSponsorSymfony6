<form method="post" action="{{ path('vendor_en_gb_delete', {'id': vendor_en_gb.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vendor_en_gb.id) }}">
    <button class="btn">Delete</button>
</form>

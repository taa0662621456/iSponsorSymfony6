<form method="post" action="{{ path('vendor_vendors_iban_delete', {'id': vendors_iban.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vendors_iban.id) }}">
    <button class="btn">Delete</button>
</form>

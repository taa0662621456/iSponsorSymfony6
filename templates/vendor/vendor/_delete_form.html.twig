<form method="post" action="{{ path('vendors_en_gb_delete', {'id': vendors_en_gb.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vendors_en_gb.id) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}VendorsIban index{% endblock %}
{% block header %}{% endblock %}
{% block article_header %}{% endblock %}
{% block body %}
    <h1>VendorsIban index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>VendorIban</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for vendors_iban in vendors_ibans %}
            <tr>
                <td>{{ vendors_iban.id }}</td>
                <td>{{ vendors_iban.vendorIban }}</td>
                <td>
                    <a href="{{ path('vendorIban_show_id', {'id': vendors_iban.id}) }}">show</a>
                    <a href="{{ path('vendorIban_edit_id', {'id': vendors_iban.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('vendorIban_new') }}">Create new</a>
{% endblock %}

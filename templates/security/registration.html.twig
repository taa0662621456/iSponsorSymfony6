{% extends 'base.html.twig' %}
{% block meta_description %}Please, sign up!{% endblock %}
{% block title %}Please, sign up!{% endblock %}
{% block stylesheets %}
    {{ encore_entry_link_tags('sign') }}
{% endblock %}
{% block body %}
    <body class="text-center">
    {% for type, message in app.flashes %}
        {% for message in message %}
{#    <div aria-live="polite" aria-atomic="true" class="bg-dark position-relative bd-example-toasts">#}
        <div class="toast-container position-absolute p-3 bottom-0 end-0">
                <div class="toast" role="alert" aria-live="assertive" aria-atomic="true">
                    <div class="toast-header">
        {#                <img src="..." class="rounded me-2" alt="...">#}
                        <strong class="me-auto">Сообщение об успешной регистрации</strong>
        {#                <small class="text-muted">11 мин. назад</small>#}
                        <button type="button" class="btn-close" data-bs-dismiss="toast" aria-label="Закрыть"></button>
                    </div>
                    <div class="toast-body">
                        {{ type }}
                        {{ message }}
                        <div class="mt-2 pt-2 border-top">
        {#                    <button type="button" class="btn btn-primary btn-sm">Ввести код подтверждения регистрации</button>#}
                            <button type="button" class="btn btn-secondary btn-sm btn-close" data-bs-dismiss="toast"></button>
                        </div>
                    </div>
                </div>
        </div>
{#    </div>#}

        {% endfor %}
    {% endfor %}
    {{ form(form) }}
    {{ form_errors (form) }}
    </body>
{% endblock %}
{% block footer %}{% endblock %}
{% block javascripts %}
    {{ encore_entry_script_tags('sign') }}
{#    <script type="module">#}
{#        import { Toast } from '@bootstrap/dist/js/bootstrap'#}

{#        Array.from(document.querySelectorAll('.toast'))#}
{#            .forEach(toastNode => new Toast(toastNode))#}
{#    </script>#}
{% endblock %}

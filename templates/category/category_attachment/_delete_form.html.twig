<form method="post" action="{{ path('category_categories_attachments_delete', {'id': categories_attachment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categories_attachment.id) }}">
    <button class="btn">Delete</button>
</form>

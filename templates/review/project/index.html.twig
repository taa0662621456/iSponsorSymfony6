{% extends 'base.html.twig' %}
{% block title %}Projects index{% endblock %}
{% block header %}{% endblock %}
{% block little_panel %}{% endblock %}
{% block article_header %}{% endblock %}
{% block section %}{% endblock %}

{% block article_content %}
    <h1>Projects index</h1>

    <table class="table">
        <thead>
        <tr>
            <th>Id</th>
            <th>Category</th>
            <th>CreateOn</th>
            <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for project in projects %}
            <tr>
                <td>{{ project.slug }}</td>
                <td>{{ project.slug }}</td>
                <td>{{ project.slug }}</td>
                <td>
                    <a href="{{ path('project_show_slug', {'id': project.slug}) }}">show</a>
                    <a href="{{ path('project_edit_slug', {'id': project.slug}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('project_new') }}">Create new</a>
{% endblock %}

{% extends 'base.html.twig' %}
{% block title %}products index{% endblock %}
{% block header %}{% endblock %}
{% block little_panel %}{% endblock %}
{% block article_header %}{% endblock %}
{% block section %}{% endblock %}

{% block article_content %}
    <h1>products index</h1>

    <table class="table">
        <thead>
        <tr>
            <th>Id</th>
            <th>Category</th>
            <th>CreateOn</th>
            <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for product in product %}
            <tr>
                <td>{{ product.slug }}</td>
                <td>{{ product.slug }}</td>
                <td>{{ product.slug }}</td>
                <td>
                    <a href="{{ path('reviewProduct_show_slug', {'slug': product.slug}) }}">show</a>
                    <a href="{{ path('reviewProduct_edit_slug', {'slug': product.slug}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('product_new') }}">Create new</a>
{% endblock %}

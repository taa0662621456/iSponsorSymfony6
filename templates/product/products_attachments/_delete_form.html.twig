<form method="post" action="{{ path('product_products_attachments_delete', {'id': products_attachment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ products_attachment.id) }}">
    <button class="btn">Delete</button>
</form>

{{ form_start(form, {attr: {'id': 'tinymce_editor','data-post-id': product.id}}) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}

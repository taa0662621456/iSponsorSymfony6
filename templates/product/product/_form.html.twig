{% form_theme form with [
    'bootstrap_5_layout.html.twig',
    'form/btn-group.html.twig']
    only %}
{{ form_errors(form) }}
{{ form(form, {attr: {'data-object':'object', 'id':'object'}}) }}
{#{{ form(form, {attr: {'data-name': product.slug, 'data-object':'object', 'data-object-id': product.id, 'id':'object'}}) }}#}

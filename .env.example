# Example .env file for Symfony + Docker

### Symfony ###
APP_ENV=dev
APP_SECRET=ChangeMeSecretKey

### Database ###
# For local Docker (override/dev)
DATABASE_URL=pgsql://symfony:symfony@database:5432/symfony

# For production
# DATABASE_URL=pgsql://symfony:symfony@localhost:5432/symfony

### JWT (if using LexikJWTAuthenticationBundle) ###
JWT_PASSPHRASE=ChangeMePassphrase

### Docker ###
DOCKER_HUB_USERNAME=your_dockerhub_user
DOCKER_HUB_ACCESS_TOKEN=your_dockerhub_token

### Mailer (if required) ###
# MAILER_DSN=smtp://localhost:1025

### Caching / Sessions ###
# REDIS_URL=redis://redis:6379
